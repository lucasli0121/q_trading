version: '3.4'

services:
  q_data_proxy:
    image: q_data_proxy:latest
    container_name: q_data_proxy
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      - TZ=Asia/Shanghai

    volumes:
      - ../q_data_proxy:/q_data_proxy